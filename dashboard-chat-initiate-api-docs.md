# Dashboard Chat Initiation API Documentation

## 📋 الغرض

تمكين الإدارة من بدء محادثات مع أي مستخدم في النظام بشكل استباقي، بدلاً من انتظار أن يبدأ المستخدم المحادثة.

---

## 🎯 السيناريو المطلوب: بدء محادثة مع مستخدم

### الـ Flow:

```
1. فتح صفحة المستخدمين → GET /api/admin/users (قائمة جميع المستخدمين)
2. البحث عن مستخدم معين → GET /api/admin/users?search=رقم_الهاتف (اختياري)
3. اختيار مستخدم → GET /api/admin/support/user/{user_id}/check (التحقق من وجود محادثة سابقة)
4. بدء/فتح المحادثة → GET /api/admin/support/{user_id} أو POST /api/admin/support/initiate
5. إرسال رسالة → POST /api/admin/support/reply
```

---

## 🔵 API Endpoints

### 1️⃣ جلب قائمة جميع مستخدمين النظام

```http
GET /api/admin/users
Authorization: Bearer {admin_token}
```

**Query Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `per_page` | integer | No | عدد النتائج في الصفحة (default: 20) |
| `page` | integer | No | رقم الصفحة |
| `search` | string | No | البحث برقم الهاتف أو الاسم |
| `has_conversation` | boolean | No | `true` = لديه محادثة سابقة، `false` = بدون محادثة، عدم إرساله = الكل |

**Request Example:**
```http
GET /api/admin/users?search=0100&per_page=20&page=1
```

**Response:**
```json
{
    "meta": {
        "page": 1,
        "per_page": 20,
        "total": 500,
        "last_page": 25
    },
    "data": [
        {
            "id": 5,
            "name": "أحمد محمد",
            "phone": "01000000000",
            "email": "ahmed@example.com",
            "created_at": "2025-01-15T10:00:00Z",
            "has_support_conversation": true,
            "last_conversation_at": "2025-12-10T09:30:00Z"
        },
        {
            "id": 10,
            "name": "سارة علي",
            "phone": "01001234567",
            "email": "sara@example.com",
            "created_at": "2025-02-20T14:00:00Z",
            "has_support_conversation": false,
            "last_conversation_at": null
        },
        {
            "id": 15,
            "name": "محمد أحمد",
            "phone": "01009876543",
            "email": null,
            "created_at": "2025-03-10T08:00:00Z",
            "has_support_conversation": true,
            "last_conversation_at": "2025-12-05T15:00:00Z"
        }
    ]
}
```

---

### 2️⃣ التحقق من وجود محادثة مع مستخدم معين

> **الغرض:** معرفة إن كان هناك محادثة دعم سابقة مع هذا المستخدم أم لا

```http
GET /api/admin/support/user/{user_id}/check
Authorization: Bearer {admin_token}
```

**Request Example:**
```http
GET /api/admin/support/user/5/check
```

**Response (في حالة وجود محادثة سابقة):**
```json
{
    "exists": true,
    "user": {
        "id": 5,
        "name": "أحمد محمد",
        "phone": "01000000000"
    },
    "conversation": {
        "id": "support-uuid-1",
        "messages_count": 8,
        "last_message_at": "2025-12-10T09:30:00Z",
        "unread_count": 2
    }
}
```

**Response (في حالة عدم وجود محادثة):**
```json
{
    "exists": false,
    "user": {
        "id": 10,
        "name": "سارة علي",
        "phone": "01001234567"
    },
    "conversation": null
}
```

---

### 3️⃣ بدء محادثة جديدة مع مستخدم (أو فتح الموجودة)

> **الغرض:** إنشاء محادثة دعم جديدة مع مستخدم ليس لديه محادثة سابقة، أو فتح المحادثة الموجودة

```http
POST /api/admin/support/initiate
Authorization: Bearer {admin_token}
Content-Type: application/json
```

**Request Body:**
```json
{
    "user_id": 10,
    "message": "مرحباً، نود إعلامك بوجود عرض خاص لك!"
}
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `user_id` | integer | ✅ Yes | معرف المستخدم المراد بدء محادثة معه |
| `message` | string | ✅ Yes | الرسالة الأولى للمحادثة |

**Response (محادثة جديدة تم إنشاؤها):**
```json
{
    "message": "تم بدء المحادثة بنجاح",
    "is_new_conversation": true,
    "data": {
        "conversation_id": "support-uuid-new",
        "user": {
            "id": 10,
            "name": "سارة علي",
            "phone": "01001234567"
        },
        "first_message": {
            "id": 1,
            "message": "مرحباً، نود إعلامك بوجود عرض خاص لك!",
            "admin_id": 1,
            "admin_name": "Admin Name",
            "created_at": "2025-12-11T10:00:00Z"
        }
    }
}
```

**Response (محادثة موجودة تم إضافة رسالة إليها):**
```json
{
    "message": "تم إرسال الرسالة بنجاح",
    "is_new_conversation": false,
    "data": {
        "conversation_id": "support-uuid-1",
        "user": {
            "id": 5,
            "name": "أحمد محمد",
            "phone": "01000000000"
        },
        "new_message": {
            "id": 15,
            "message": "مرحباً، نود إعلامك بوجود عرض خاص لك!",
            "admin_id": 1,
            "admin_name": "Admin Name",
            "created_at": "2025-12-11T10:00:00Z"
        }
    }
}
```

---

## 🖥️ واجهة المستخدم المقترحة

### صفحة اختيار المستخدم لبدء المحادثة:

```
┌─────────────────────────────────────────────────────────────────────┐
│  🔍 البحث عن مستخدم                                                  │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │ 📱 ابحث برقم الهاتف أو الاسم...                                │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  📋 فلترة: [ 🔘 الكل ] [ ⚪ لديه محادثة ] [ ⚪ بدون محادثة ]         │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│  قائمة المستخدمين                                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 👤 أحمد محمد                                                 │   │
│  │    📱 01000000000                                            │   │
│  │    💬 آخر محادثة: 10/12/2025                                 │   │
│  │                                          [ 💬 فتح المحادثة ]  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 👤 سارة علي                                                  │   │
│  │    📱 01001234567                                            │   │
│  │    ⚠️ لا يوجد محادثة سابقة                                    │   │
│  │                                          [ ✉️ بدء محادثة ]    │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 👤 محمد أحمد                                                 │   │
│  │    📱 01009876543                                            │   │
│  │    💬 آخر محادثة: 05/12/2025                                 │   │
│  │                                          [ 💬 فتح المحادثة ]  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│                         [ ◀ السابق ] صفحة 1 من 25 [ التالي ▶ ]       │
└─────────────────────────────────────────────────────────────────────┘
```

### نافذة بدء محادثة جديدة (Modal/Dialog):

```
┌─────────────────────────────────────────────────────────────────┐
│  ✉️ بدء محادثة مع: سارة علي (01001234567)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  اكتب رسالتك:                                                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                                                           │  │
│  │ مرحباً، نود إعلامك بوجود عرض خاص لك!                     │  │
│  │                                                           │  │
│  │                                                           │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│                              [ إلغاء ]    [ 📤 إرسال وبدء ]     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📋 ملخص الـ Endpoints

| الوظيفة | Method | Endpoint |
|---------|--------|----------|
| جلب جميع المستخدمين | `GET` | `/api/admin/users` |
| البحث عن مستخدم | `GET` | `/api/admin/users?search=...` |
| التحقق من وجود محادثة | `GET` | `/api/admin/support/user/{user_id}/check` |
| بدء/فتح محادثة مع رسالة | `POST` | `/api/admin/support/initiate` |
| عرض محادثة مستخدم | `GET` | `/api/admin/support/{user_id}` |
| الرد على مستخدم | `POST` | `/api/admin/support/reply` |

---

## 🔄 سيناريو الاستخدام الكامل

### السيناريو 1: بدء محادثة مع مستخدم جديد

```
1. الأدمن يفتح صفحة "المستخدمين" / "بدء محادثة"
2. يبحث عن المستخدم برقم الهاتف: 01001234567
3. يجد سارة علي (لا يوجد محادثة سابقة)
4. يضغط "بدء محادثة"
5. تظهر نافذة كتابة الرسالة الأولى
6. يكتب الرسالة ويضغط "إرسال"
7. يتم توجيهه لصفحة المحادثة مع سارة
```

### السيناريو 2: استكمال محادثة موجودة

```
1. الأدمن يفتح صفحة "المستخدمين"
2. يبحث عن المستخدم برقم الهاتف: 01000000000
3. يجد أحمد محمد (لديه محادثة سابقة - آخرها 10/12)
4. يضغط "فتح المحادثة"
5. يتم توجيهه مباشرة لصفحة المحادثة الموجودة
```

---

## ⚠️ ملاحظات مهمة

1. **الـ Endpoint الموحد:** استخدام `POST /api/admin/support/initiate` يتعامل مع الحالتين:
   - إذا لم تكن هناك محادثة → يُنشئ واحدة جديدة
   - إذا كانت هناك محادثة → يضيف الرسالة للمحادثة الموجودة

2. **إرسال Notification:** عند بدء محادثة جديدة أو إرسال رسالة، يجب إرسال Push Notification للمستخدم

3. **Real-time Updates:** استخدام WebSocket/Laravel Reverb لتحديث المحادثة في الوقت الحقيقي

4. **الصلاحيات:** هذه الـ endpoints تحتاج صلاحية `support` أو `admin`

---

## 🔐 Headers المطلوبة

```http
Authorization: Bearer {admin_token}
Content-Type: application/json
Accept: application/json
```

---

> **تاريخ الإنشاء:** 2025-12-11
> 
> **الغرض:** تمكين الإدارة من التواصل الاستباقي مع المستخدمين
