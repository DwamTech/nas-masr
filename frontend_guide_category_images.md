دليل المطور (Frontend) - إدارة الصورة الافتراضية للقسم
تم إضافة ميزة جديدة تتيح للأدمن تعيين صورة افتراضية لكل قسم (مثل الأطباء، الوظائف، إلخ). هذه الصورة ستستخدم في التطبيق إذا لم يقم المعلن برفع صورة للإعلان.

📡 API Updates
1. عرض الأقسام (GET)
Endpoint: GET /api/admin/categories

كل كائن قسم (Category Object) يحتوي الآن على حقل جديد:

default_image: اسم الملف (String/Null).
default_image_url: الرابط الكامل للصورة (String/Null).
{
    "id": 1,
    "name": "الأطباء",
    "icon_url": "...",
    "default_image_url": "https://domain.com/storage/uploads/categories/xyz.jpg" // <--- رابط الصورة
}
2. إضافة/تعديل قسم (POST / PUT)
Endpoint:

POST /api/admin/categories (إنشاء)
PUT /api/admin/categories/{id} (تعديل)
يجب إرسال الـ Request بصيغة multipart/form-data لدعم رفع الملفات.

الحقل	النوع	الوصف
name	String	اسم القسم
default_image	File	(اختياري) ملف الصورة الجديدة
_method	String	"PUT" (فقط عند التعديل لأن Laravel لا يدعم رفع الملفات مع PUT مباشرة)
مثال للـ Form Data عند التعديل:

_method: PUT
name: وظائف
default_image: (Binary File)
3. حذف الصورة (Delete Image)
لحذف الصورة الحالية، قم بإرسال الحقل remove_default_image بقيمة 1 أو true ضمن طلب التعديل (PUT).

مثال للـ Form Data عند الحذف:

_method: PUT
remove_default_image: 1
🛠️ المطلوب في واجهة الأدمن
عرض الصورة: في جدول الأقسام أو صفحة التفاصيل، اعرض الصورة المصغرة من default_image_url إن وجدت.
رفع الصورة: أضف حقل File Input باسم "الصورة الافتراضية" في مودال الإضافة/التعديل.
حذف الصورة: أضف زر أو Checkbox "حذف الصورة الحالية" يظهر فقط إذا كان هناك default_image_url ومفعل.
تلميح: "هذه الصورة ستظهر للإعلانات التي لا تحتوي على صور في هذا القسم".