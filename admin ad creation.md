# دليل التنفيذ: إضافة إعلان بواسطة الأدمن (Admin Ad Creation)

هذا المستند موجه لفريق الـ Frontend لتوضيح كيفية التعامل مع ميزة "إضافة إعلان" خاصةً عندما يكون المستخدم الحالي **Admin**.

---

## 🚀 نظرة عامة (Overview)

النظام يمنح الأدمن صلاحيات "Super" عند إنشاء الإعلانات:
1. **تجاوز الدفع**: لا تظهر خطوات الدفع أو اختيار الباقة للأدمن.
2. **تجاوز الحدود**: يمكن للأدمن إضافة عدد غير محدود من الإعلانات.
3. **النشر الفوري**: الإعلان يُنشر فوراً (`Valid`) دون انتظار مراجعة.

---

## 📡 Endpoint Details

- **URL**: `POST /api/v1/{section_slug}/listings`
- **Method**: `POST`
- **Auth**: `Bearer Token` (Must be Admin)
- **Headers**:
  - `Content-Type: multipart/form-data` (بسبب رفع الصور)
  - `Accept: application/json`

### المعاملات الديناميكية (Route Parameters)
يجب تغيير `{section_slug}` بناءً على القسم المختار:
- `cars` (سيارات)
- `real_estate` (عقارات)
- `jobs` (وظائف)
- `doctors` (أطباء)
- `teachers` (معلمين)
- `sale` (بيع وشراء - عام)
...وغيرها.

---

## 📦 هيكل البيانات (Payload Structure)

البيانات المرسلة تنقسم إلى جزء ثابت (مشترك بين الكل) وجزء متغير (حسب القسم).

### 1️⃣ البيانات المشتركة (Common Fields)

هذه الحقول ترسل مع **كل** الأقسام:

| الحقل | النوع | إلزامي؟ | الوصف |
| :--- | :--- | :--- | :--- |
| `title` | String | ✅ | عنوان الإعلان (مطلوب في أقسام معينة، يولد تلقائياً في السيارات) |
| `description` | String | ✅ | وصف تفصيلي |
| `price` | Number | ✅ | السعر (أو الراتب في الوظائف) |
| `phone` | String | ✅ | رقم الهاتف للتواصل |
| `governorate_id` | Int | ✅ | ID المحافظة |
| `city_id` | Int | ✅ | ID المدينة |
| `main_image` | File | ✅ | الصورة الرئيسية (إلزامي للسيارات/العقارات/البيع) |
| `images[]` | Files | ❌ | صور إضافية (Gallery) |
| `plan_type` | String | ❌ | نوع الإعلان: `free`, `standard`, `featured` (أرسل أي قيمة، النظام سيقبلها مجاناً للأدمن) |

### 2️⃣ البيانات الخاصة بكل قسم (Section Specifics)

#### 🚗 قسم السيارات (`cars`)
يحتاج بيانات التصنيف (Make/Model) وحالة السيارة.

```json
{
  "make_id": 1,        // موديل السيارة (تويوتا)
  "model_id": 5,       // الماركة (كورولا)
  "year": 2023,        // سنة الصنع
  "transmission": "automatic", // نوع الناقل
  "fuel_type": "gas",  // نوع الوقود
  "odometer": 50000,   // العداد
  "condition": "used"  // الحالة
}
```

#### 🏠 قسم العقارات (`real_estate`)
يحتاج بيانات القسم الفرعي (شقق، فيلات..) والخصائص.

```json
{
  "main_section_id": 10, // بيع أو إيجار
  "sub_section_id": 15,  // شقة، فيلا..
  "area": 150,           // المساحة (متر)
  "rooms": 3,            // عدد الغرف
  "bathrooms": 2,        // حمامات
  "floor_level": 5,      // الدور
  "finishing_type": "super_lux" // التشطيب
}
```

#### 💼 قسم الوظائف (`jobs`)
يحتاج تحديد نوع الوظيفة والمجال.

```json
{
  "main_section_id": 20, // المجال (برمجة، طب..)
  "sub_section_id": 25,  // التخصص (Frontend, Backend..)
  "job_type": "full_time", // دوام كامل/جزئي
  "experience_level": "mid_senior" // مستوى الخبرة
}
```

---

## ⚙️ كيفية التعامل في الـ Frontend (Senior Guidelines)

### 1. تجاهل خطوات الدفع (Payment Skip)
بما أن المستخدم `Admin`، لا تقم بتوجيهه لصفحة الدفع بعد ملء الاستمارة.
- **للمستخدم العادي**: Form -> Select Plan -> Payment Gateway -> Success
- **للأدمن**: Form -> Submit API -> Success Screen مباشـرة.

### 2. أنواع الإعلانات (Plan Selection)
الأدمن يمكنه اختيار جعل الإعلان "متميز" (`featured`) مجاناً.
- اقترح إضافة `Dropdown` أو `Radio` في فورم الأدمن يسأله: **"نوع الإعلان؟"**
  - [ ] مجاني
  - [ ] متميز (Featured)
  - [ ] قياسي (Standard)
- عند الاختيار، أرسل القيمة في حقل `plan_type`.

### 3. التعامل مع الـ Response
النجاح سيعود بـ `HTTP 200/201`.
تحقق من `response.data.payment.type`:
- إذا كان `admin_bypass`، فهذا تأكيد أن الإعلان تم نشره بصلاحيات الأدمن دون دفع.
- اعرض رسالة نجاح: *"تم نشر الإعلان بنجاح (وضع المسؤول)"*.

---

## ⚠️ ملاحظات هامة
- **الصور الافتراضية**: في أقسام الوظائف والأطباء، إذا لم يرفع الأدمن صورة `main_image`، النظام سيضع صورة افتراضية. في السيارات والعقارات الصورة إجبارية.
- **التصنيفات**: تأكد من تحميل الـ `categories`، `makes`، `models`، `governorates` الصحيحة بناءً على الـ `section_slug` قبل فتح الفورم.
